<div id="piechart"></div>

<gvisualization:pieCoreChart 
	dynamicLoading="${true}" 
	elementId="piechart" 
	title="${title}" 
 	width="${width}" 
 	height="${height}" 
 	columns="${columns}" 
 	data="${data}"/>